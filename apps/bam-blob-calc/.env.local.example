# Server-side RPC used to fetch eth_feeHistory.
# Any Ethereum mainnet JSON-RPC works. Falls back to a public endpoint if unset.
RPC_URL=https://ethereum-rpc.publicnode.com

# Number of recent blocks averaged for the fee panel (default 20).
FEE_HISTORY_BLOCKS=20
