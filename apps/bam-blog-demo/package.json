{
  "name": "bam-blog-demo",
  "version": "0.1.0",
  "private": true,
  "description": "Tiny static-HTML demo for the bam-comments widget. Two hand-authored pages embed the widget through its public <script> contract — exactly what an external host would write.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest",
    "test:run": "vitest --run"
  },
  "dependencies": {
    "bam-comments": "workspace:*"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "vitest": "^1.2.0"
  }
}
